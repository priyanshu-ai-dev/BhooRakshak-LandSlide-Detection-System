import DashboardLayout from './components/layout/DashboardLayout'

/**
 * App — root component.
 * Phase 0: renders only the dashboard shell.
 * Future phases will add a router (React Router) here.
 */
export default function App() {
  return <DashboardLayout />
}
